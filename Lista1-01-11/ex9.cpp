#include <iostream>
#include <cmath>
using namespace std;

int main() 
{
    float cat1, cat2, hipo;
    cin >> cat1 >> cat2;
    hipo = sqrt(pow(cat1,2)+pow(cat2,2));
    cout << hipo; 
    return 0;
}

